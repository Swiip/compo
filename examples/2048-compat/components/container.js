import { component, withStyle, css } from '../../..';

component(
  'swiip-container',
  withStyle(() => css`
    swiip-container {
      display: block;
      width: 500px;
      margin: 0 auto;
    }
  `),
);
